# EDA (Exploratary Data Analysis)

## Description
This repo have only the EDA. It is for practise of EDA.

## Setup
```bash
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt